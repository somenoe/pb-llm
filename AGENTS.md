# Rules

- Run lint every code change
- Expect no linting errors or warnings

## Commands

- `just run` - runs the project
- `just lint` - checks for linting errors
- `just build` - builds the project
